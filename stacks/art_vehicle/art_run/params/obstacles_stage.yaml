# local obstacle grid parameters -- stage simulation
# $Id$

costmap:
    
    # sensor sources
    observation_sources: front_sick roadmap_cloud

    front_sick:
    
        clearing: true
        data_type: LaserScan
        expected_update_rate: 0.2           # twice actual frequency
        marking: true
        observation_persistence: 1.0
        obstacle_range: 100.0                # ignore closer readings
        raytrace_range: 100.0
        #sensor_frame: /front_sick

# local obstacle grid Velodyne parameters
# $Id$

costmap:
    
    # sensor sources
    observation_sources: velodyne_obstacles roadmap_cloud

    velodyne_obstacles:
    
        clearing: true
        data_type: PointCloud
        expected_update_rate: 0.2           # twice actual frequency
        marking: true
        observation_persistence: 0.2
        obstacle_range: 80.0                # max readings range
        raytrace_range: 80.0
        #sensor_frame: /velodyne
        topic: velodyne/obstacles

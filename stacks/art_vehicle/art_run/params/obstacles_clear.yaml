# local obstacle grid clearing point cloud
# $Id$

costmap:
    
    obstacles_clear:
    
        max_obstacle_height: 10.0
        min_obstacle_height: -10.0
        clearing: true
        data_type: PointCloud
        expected_update_rate: 0.5
        marking: false
        observation_persistence: 0.0
        obstacle_range: 80.0                # max readings range
        raytrace_range: 80.0
        sensor_frame: /vehicle
